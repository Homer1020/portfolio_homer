---
interface Props extends astroHTML.JSX.SelectHTMLAttributes {
  label?: string
}

const {label, id, ...props} = Astro.props
---
<div>
  <label class="label" for={id}>{label}</label>
  <select class="input" {...props} id={id}>
    <slot />
  </select>
</div>